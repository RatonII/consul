pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                git 'https://github.com/RatonII/consul.git'
                sh 'ls -la'
                echo 'Building..'
                sh 'docker build -t image:test .'
                cleanWs()
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}
